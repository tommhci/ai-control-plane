@AGENTS.md
@AI_BOOTSTRAP.md
@governance/agent-constitution.md

## Claude Code

- Read `NODE_QUEUE.md` and `HANDOVER.md` before starting any task.
- Treat `HANDOVER.md` as the session snapshot; refresh it when the active node or gate changes.
- If `HANDOVER.md` conflicts with `AGENTS.md`, `NODE_QUEUE.md`, or `governance/`, assume the snapshot is stale and re-check the source files.
